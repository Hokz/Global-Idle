node_modules/
dist/
*.tsbuildinfo
coverage/
.next/
out/

# Generated Prisma client — never committed (§4.3).
packages/domain/src/generated/

# Built content bundles — produced by `pnpm --filter @global-idle/game-data run build`.
packages/game-data/bundles/

.env
.env.local
*.log
.DS_Store

# Playwright run output (traces, screenshots, the last-run marker). Evidence of
# a run, never source.
test-results/
playwright-report/
blob-report/

# Phase 3.7 — private asset override. User-supplied Tibia client assets are a
# PRIVATE technical reference, never redistributable content (spec §9). The
# public build ships distributable placeholders and must pass with none of this
# present; anything here is a local convenience only.
apps/web/public/assets/private/
private-assets/
*.rcc
*.rar
