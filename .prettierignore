node_modules/
dist/
coverage/
.next/
pnpm-lock.yaml
packages/domain/src/generated/
packages/domain/prisma/migrations/

# Approved documents are NOT Prettier's to reformat.
# The Phase 0B specification and the 18 ACCEPTED ADRs are governed content:
# running `pnpm format` over them rewrote 43 files and 1,295 lines of the spec
# on first use. Prettier's job in this repository is CODE.
*.md
docs/
FOUNDATION_MANIFEST.json

# Generated by Next.js, and it says so: "This file should not be edited".
# Prettier rewrites it, Next regenerates it, and format:check never settles.
apps/web/next-env.d.ts

# A BYTE-EXACT golden artifact, not source. E1 and E2 compare the engine's
# output against these bytes; a formatter that adds a trailing newline breaks
# a determinism test for a reason that has nothing to do with determinism.
tests/fixtures/engine/golden.json
